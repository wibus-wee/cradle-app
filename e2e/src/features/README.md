<!-- Once this directory changes, update this README.md -->

# E2E/Features

Essence (精华) user journeys only. Retired scenarios live in `e2e/_archive/features/`.

## Active

- **chat.feature**: Standard multi-turn, stop, provider error, reload recovery, reasoning, session lifecycle
- **claude-agent.feature**: Real Claude Agent SDK approval allow/deny via Anthropic Messages simulator
- **workspace-kanban.feature**: Workspace add + Overview, kanban/issue create path

## Tags

- `@essence` — full curated suite
- `@P0` — smoke subset
- `@P1` — deeper side paths within the essence set
