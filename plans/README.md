# Implementation Plans

Generated by the improve skill on 2026-07-05, planned against commit `ac47f3b`.
Source: full read-only audit of `apps/web` and `apps/server` across correctness,
security, performance, tech-debt/architecture, and test coverage.

Each executor: read the plan fully before starting, run its drift check, honor its
STOP conditions, and update your row below when done. Plans are self-contained —
they do not assume you saw the audit or any other plan.

## Execution order & status

Ordered by leverage (security/correctness first, structural refactors last).

| Plan | Title                                                    | Priority | Effort | Depends on | Status                                                                                 |
| ---- | -------------------------------------------------------- | -------- | ------ | ---------- | -------------------------------------------------------------------------------------- |
| 001  | Fix embedded browser (WebContentsView) flicker/occlusion | P2       | L      | —          | DONE                                                                                   |
| 002  | Add auth to HTTP + WebSocket surface                     | P1       | L      | —          | DONE                                                                                   |
| 003  | Require auth on relay-tunneled traffic                   | P1       | M      | 002        | DONE                                                                                   |
| 004  | Constrain filesystem browse + shell cwd to roots         | P1       | S      | 002        | DONE                                                                                   |
| 005  | Close SSRF gaps (link-preview + provider-catalog)        | P1       | M      | —          | DONE                                                                                   |
| 006  | Stop returning relay pairing codes on read routes        | P1       | S      | 002        | BLOCKED (drift check: relay-transport changed since ac47f3b)                           |
| 010  | Fix issue-agent run-tracking races + delegation tx       | P1       | M      | —          | DONE                                                                                   |
| 013  | Include apps/web tests in root test run                  | P1       | S      | —          | DONE                                                                                   |
| 014  | Crash-safe SSE/WS handlers + cursor-correct reconnect    | P1       | M      | —          | DONE                                                                                   |
| 007  | Secrets key versioning + rotation path                   | P2       | M      | —          | DONE                                                                                   |
| 008  | Harden server plugin loading (provenance + confirm)      | P2       | L      | —          | DONE                                                                                   |
| 009  | Gate worktree setup-hook execution behind confirmation   | P2       | S      | —          | DONE                                                                                   |
| 011  | Coalesce check-then-act races on registries              | P2       | M      | —          | BLOCKED (drift check: relay/conversation transport modules changed since ac47f3b)      |
| 012  | Terminal cleanup + error handling on run/boot paths      | P2       | M      | —          | DONE                                                                                   |
| 015  | Add route-level error boundaries                         | P2       | M      | —          | DONE                                                                                   |
| 016  | Version + validate persisted Zustand stores              | P2       | M      | —          | DONE                                                                                   |
| 017  | Reduce chat streaming render cost                        | P2       | M      | —          | DONE                                                                                   |
| 018  | Code-split heavy panels and routes                       | P2       | M      | —          | BLOCKED (drift check: settings/chat feature files changed since ac47f3b)               |
| 019  | Rewrite apps/server/AGENTS.md to match Elysia            | P2       | S      | —          | DONE                                                                                   |
| 022  | Characterization tests for untested web paths            | P2       | M      | 013        | BLOCKED (test seams: driver hook and file-tree live sync require large mocks/refactor) |
| 020  | Split the largest god files                              | P3       | L      | 022        | BLOCKED (depends on blocked Plan 022 characterization tests)                           |
| 021  | Restore server dependency direction (infra≠modules)      | P3       | M      | 019        | BLOCKED (import graph has additional http/plugins -> modules deps beyond plan scope)   |
| 023  | Consolidate web data-fetching onto generated hooks       | P3       | L      | 022        | BLOCKED (depends on blocked Plan 022 characterization tests)                           |
| 024  | Rebuild the chat core on native Event Sourcing           | P2       | XL     | —          | DONE                                                                                   |
| 025  | Authoritative usage fields per runtime provider          | P1       | M      | —          | TODO                                                                                   |
| 026  | Publish plugin SDK + open marketplace to any repo        | P2       | M      | —          | DONE                                                                                   |
| 027  | Persisted, live-reloadable external plugin sources        | P1       | L      | 026        | DONE                                                                                   |
| 028  | Mirror dynamic plugin sources into the desktop layer      | P2       | M      | 027        | DONE                                                                                   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale).

## Follow-up notes

- 2026-07-05, Plan 001: `DesktopBrowserManager` now updates already-attached `WebContentsView` bounds without re-adding the child view, hides attached views via `setVisible(false)`, and keeps destruction removal on the runtime cleanup path. Renderer bounds sync now uses one rAF frame and no longer pauses for ordinary layout drag/animation; `nativeBoundsPaused` is retained only for browser-panel closing. Native surface layering is centralized through a reference-counted suppression store, browser address suggestions use occlusion props, and global `Dialog` / `AlertDialog` / `Sheet` primitives suppress the native browser surface while open. Legacy browser backend/script/annotation-overlay files were already absent; `setBorderRadius` was confirmed present in Electron 42.4.1 types but skipped because there is no current native viewport radius requirement. Verification passed `pnpm typecheck`, `pnpm test` (245 files / 1345 tests), full `pnpm --filter @cradle/web test` (71 files / 302 tests), focused browser web tests, root Vitest for `apps/desktop/src/main/browser-manager.test.ts`, local ESLint for `browser-panel.test.tsx`, and `git diff --check`. `pnpm lint` was run and still fails on existing repository-wide lint debt outside Plan 001 paths; `react-doctor --diff` exits 1 on existing `file-tree.tsx` / `await-panel.tsx` findings but remains at score 70 with no Plan 001 file findings. The originally listed desktop filtered Vitest command is not usable in this repo state because it resolves root project config paths from `apps/desktop`.
- 2026-07-05, Plan 013: root `pnpm test` now includes `node`, `apps/server`, and `apps/web` Vitest projects. The newly exposed `src/components/editor/markdown-editor.test.tsx` i18n provider gap was fixed, and root test now passes 238 files / 1310 tests.
- 2026-07-05, Plan 014: server tail replay already catches missed session/global events from persisted cursors and sends `SnapshotRequired` on large gaps, so cursor-refresh URL rebuilding was downgraded to snapshot/list/runtime catch-up on tail error. Malformed workspace file, Chronicle download-progress, and PTY frames are now dropped without escaping handler boundaries.
- 2026-07-05, Plan 007: `agent_credentials.key_version` was added with a non-destructive migration. Secrets now use `vN:iv:payload:tag` envelopes while still reading legacy `iv:payload:tag`; rotation re-encrypts all credential rows inside one transaction and intentionally has no CLI exposure because it accepts master secrets.
- 2026-07-05, Plan 008: external local plugin activation is now gated by a checksum-bound operator trust grant in `trust_grants` (subject_type=plugin_package); `PATCH /plugins/:routeSegment/enabled` records the grant for the currently discovered package revision. External local plugins remain blocked while relay host enrollments expose the server. Marketplace install receipts can optionally provide `packageChecksum`; mismatches are refused, while existing receipts without checksum keep provenance/permission behavior and are noted as an integrity gap rather than treated as verified.
- 2026-07-05, Plan 009: `.cradle/worktrees.json` setup hooks now require a workspace-scoped trust grant in `trust_grants` (subject_type=worktree_setup_hook) or an explicit `confirmedSetupHooks` flag on worktree creation. Untrusted or relay-exposed runs return warnings that list skipped commands and do not execute them.
- 2026-07-05, Plan 012: run finalization now releases active runs and drains the queue from a guarded `finally`; boot relay/conversation tasks now attach non-fatal `.catch` loggers. Provider runtime disposal failures record `PROVIDER_RUNTIME_DISPOSAL_FAILED`, and OpenCode event subscription failures log, emit observability, and fail fast when recovery cannot start. Verification passed `pnpm --filter @cradle/server typecheck`, focused `opencode provider-runtime turn-executor` tests, and a retried full `pnpm --filter @cradle/server test` run (135 files / 807 tests); the first full run hit unrelated `diff-review` 5s timeouts that passed when run focused.
- 2026-07-05, Plan 015: added a shared `RouteErrorFallback` with route retry and home navigation, wired it as the TanStack router `defaultErrorComponent`, and added scoped error components for chat split workspaces, workspace/browser-panel host, plugin panel host, diff review, workspace diffs, and kanban. Common i18n resources now include route-error strings for all supported locales. Verification passed `pnpm --filter @cradle/web typecheck`, focused route-error/i18n tests, and full `pnpm --filter @cradle/web test` (67 files / 285 tests).
- 2026-07-05, Plan 016: `surface-store` and `browser-panel` now define explicit persist versions, validate old unversioned payloads during merge, and validate versioned mismatches during migrate. Corrupt persisted surfaces fall back to home, corrupt browser history/collapsed maps reset to empty maps, and fixture tests cover old/corrupt payloads. Verification passed `pnpm --filter @cradle/web typecheck`, focused store tests, and full `pnpm --filter @cradle/web test` (68 files / 291 tests).
- 2026-07-05, Plan 017: hot chat render-plan classifier reads now use a descriptor cache invalidated by tool identity, state, input/output identity, arguments text, and error text. The streaming-message-id selector now reuses a shared idle Set and preserves the active Set reference while membership is unchanged. Verification passed `pnpm --filter @cradle/web typecheck`, focused classifier/store tests, and full `pnpm --filter @cradle/web test` (69 files / 294 tests).
- 2026-07-05, Plan 019: `apps/server/AGENTS.md` now describes the actual Elysia composition root, runtime bootstrap, `infra.ts`, HTTP plugins, `AppError`/error mapping, TypeBox/OpenAPI/`x-cradle-cli` route metadata, lifecycle cleanup, and real server verification scripts. Verification checked that old framework references were removed and documented paths exist.
- 2026-07-05, Plan 022: partial characterization coverage landed for Chronicle download-progress parsing/EventSource behavior, sync-socket pending subscribe/malformed-frame/reconnect cursor behavior, and PTY pending queue/reconnect cursor behavior. The sync-socket test exposed and fixed a delayed initial-subscribe race that could resend a stale cursor after reconnect. The remaining driver hook and file-tree live-sync targets are blocked because meaningful tests require large mocks or extracting seams first. Verification passed `pnpm --filter @cradle/web typecheck`, focused new tests, and full `pnpm --filter @cradle/web test` (71 files / 301 tests).
- 2026-07-06, Plan 024 M0: added `apps/server/src/modules/chat-runtime/es/parity.ts` and `apps/server/scripts/chat-es-parity.ts` to replay a session event log through the real projectors inside a rollback-only transaction and diff `messages`, `backend_runs`, and `chat_session_queue_items`. Focused characterization covers clean replay, queue claim/terminal projection, steer, rollback, persisted-run abort, interrupted-run recovery, expected streaming/import bypasses, and unexplained backend-run drift. Verification passed `pnpm --filter @cradle/server exec vitest run src/modules/chat-runtime/es/parity.test.ts`, `pnpm --filter @cradle/server typecheck`, and an earlier full `pnpm --filter @cradle/server test -- src/modules/chat-runtime/es/parity.test.ts` invocation that ran the full server suite (136 files / 812 tests).
- 2026-07-06, Plan 024 M1-M2: event payload contracts are now owned in `es/events.ts` without Drizzle row payload imports, stored payload JSON is written with `v: 2`, v1 payloads upcast through `parseStoredChatSessionEvent`, and the old `assistantMessageProjection` hint is only read by the v1 upcaster. `es/aggregate.ts` now exposes `evolve`, `es/decide.ts` owns active-run / terminal-run / queue-claim / rollback invariants, and command commits append with `expectedVersion` through the session actor. Verification passed `pnpm --filter @cradle/server typecheck`, `pnpm --filter @cradle/server exec vitest run src/modules/chat-runtime/es`, and `git diff --check`. A full `pnpm --filter @cradle/server test` run was attempted but did not return a captured final summary after several minutes; no lingering Vitest process remained, so it is not counted as a pass.
- 2026-07-06, Plan 024 M3-M5 partial: non-streaming direct projection writes now flow through facts: `PlanImplementationResponded`, `MessageImported`, and `QueueItemProviderTargetCleared`. `persistMessageSnapshot` was removed. Added `es/rebuild.ts` plus `scripts/chat-es-rebuild.ts` for per-session projection rebuild, changed terminal drift recovery to replay stored terminal facts through the projector, and added recovery bootstrap facts for historical projection-only queue/run rows before terminal repair. Documented versioned facts, `decide/evolve`, `expectedVersion`, rebuild tooling, recovery bootstrap, and current `session_events` retention. Local replay profile against the configured DB top sessions: 14,073 events read in 44.875ms and reduced in 3.618ms; 1,897 events read in 28.155ms and reduced in 0.504ms, so no aggregate-state cache was added. Verification passed `pnpm --filter @cradle/server exec vitest run src/modules/chat-runtime/es`, `pnpm --filter @cradle/server exec vitest run tests/chat-runtime-recovery.test.ts --maxWorkers=1 --reporter=dot`, `pnpm --filter @cradle/server exec vitest run tests/chat-runtime.test.ts --maxWorkers=1 --reporter=dot`, `pnpm --filter @cradle/server typecheck`, `pnpm --filter @cradle/web typecheck`, `git diff --check`, the direct-write grep excluding projectors/parity/rebuild (only `active-run-stream.ts` remains), and full `pnpm --filter @cradle/server exec vitest run --maxWorkers=1 --reporter=dot` (139 files / 827 tests).
- 2026-07-06, Plan 024 M3 Option A: active streaming partial snapshots now append `AssistantMessageSnapshotted` facts through the session actor and projector instead of updating `messages` directly; duplicate compacted snapshots are skipped. Event tail/contracts/web sync include the new slim message-refresh event, and parity now treats logless streaming message mutations as unexplained drift. Focused verification passed `pnpm --filter @cradle/server exec vitest run src/modules/chat-runtime/es --maxWorkers=1 --reporter=dot`, `pnpm --filter @cradle/server exec vitest run tests/chat-runtime.test.ts tests/chat-runtime-recovery.test.ts --maxWorkers=1 --reporter=dot`, and the direct-write grep excluding projectors/parity/rebuild with no output.
- 2026-07-06, Plan 024 complete: final verification passed `pnpm --filter @cradle/server typecheck`, `pnpm --filter @cradle/web typecheck`, full `pnpm --filter @cradle/server exec vitest run --maxWorkers=1 --reporter=dot` (139 files / 832 tests), root `pnpm test` (249 files / 1369 tests), `git diff --check`, and the direct-write grep excluding projectors/parity/rebuild with no output.
- 2026-07-06, Plans 026-028: `@cradle/plugin-sdk` is now publishable from `dist` with public npm publish config; marketplace install links accept any valid GitHub `owner/repo`, repo-root or normalized relative plugin paths, and scoped/unscoped npm package names while preserving checksum and install-confirmation gates. Added persisted `plugin_sources` storage, source installer support for local path / GitHub tarball / `npm pack`, live server discovery/removal APIs and generated CLI commands, Settings source add/remove UI, and desktop source mirroring through server HTTP plus preload IPC. New persisted-source plugins are registered as `externalLocal` and disabled until operator enable records the existing checksum trust grant. Verification passed `pnpm --filter @cradle/plugin-sdk build`, `pnpm --filter @cradle/plugin-sdk typecheck`, `pnpm --filter @cradle/server typecheck`, `pnpm --filter @cradle/web typecheck`, `pnpm --filter @cradle/desktop typecheck`, `pnpm --filter @cradle/cli typecheck`, `pnpm --filter @cradle/cli cradle --help`, focused Vitest for desktop plugin install/loader, focused server loader Vitest, and full web Vitest (`71 files / 303 tests`). `pnpm --filter @cradle/server test -- src/plugins src/modules/plugins` was attempted but the repo's broad Vitest matching ran unrelated relay/WebSocket/preferences suites and failed on sandboxed `127.0.0.1` listeners plus pre-existing 500 assertions outside the plugin-source path; it is not counted as a pass. npm publish itself was not executed; manual publish from `packages/plugin-sdk` remains the release step.

## Recommended sequencing

1. **Security first** (002 → 003, 004, 006; 005 in parallel): the relay feature makes
   the "local-only, no-auth" assumption false. 002 is the keystone; 003/004/006 build on it.
2. **Correctness that requires a restart to recover** (010, 012, 011, 014): run-state
   corruption, terminal-cleanup leaks, registry races, and sync reconnect gaps.
3. **Test baseline** (013 then 022): make root `pnpm test` meaningful for the frontend,
   then add a net under the paths that later refactors touch.
4. **Perf + hardening + hygiene** (007, 008, 009, 015, 016, 017, 018, 019).
5. **Structural refactors last** (020, 021, 023): high value, but must follow the test
   net and the security/correctness fixes.

## Dependency notes

- 003 requires 002 (relay tokens layer on the base auth boundary).
- 004 and 006 assume 002's auth boundary as the primary protection (they are defense-in-depth / info-leak fixes on top).
- 022 requires 013 so the new web tests actually run in the main suite.
- 020 (god-file splits) requires 022's characterization tests for the web targets — don't refactor untested behavior blind.
- 021 pairs with 019 so the AGENTS.md dependency rules and the code agree.
- 023 (data-fetching) should follow 022 (chronicle test net) and ideally 020's chronicle split.
- 027 depends on 026 (naming/repo openness) but is independently useful even if 026 hasn't landed.
- 028 depends on 027 (needs the persisted source registry + its HTTP routes to exist before desktop can mirror it).

## Plugin external-loading track (026-028)

Ordered by the user's explicit decisions: publish the SDK to npm, defer sandboxing, allow
installing from any repo. 026 removes the naming/repository allowlists and makes
`@cradle/plugin-sdk` npm-installable. 027 adds a persisted `pluginSources` registry with
git/npm/local-path installers and a Settings UI, so server+web plugin layers can be added
and removed live, with no restart. 028 closes the remaining gap for `cradle.desktop`
capabilities by having the desktop process pull resolved source state from the
already-running server over HTTP instead of duplicating the installer. None of these three
add process isolation for `activate()` — that remains full in-process trust, gated only by
the existing Plan 008 checksum-bound operator grant. A follow-up sandboxing plan should be
written separately if that decision changes.

## Effect.TS / framework-migration question — considered and rejected

A full migration to Effect.TS (or another effect-system framework) was evaluated and
**rejected**. Rationale: the codebase's actual pain points — missing security boundaries,
9000-line god files, and scattered check-then-act races — are not what an effect system
fixes. Migration is viral (any `Effect<A,E,R>` return type propagates through the call
chain) and would require rewriting ~2100 source files deeply integrated with Elysia,
Drizzle, and three AI-provider SDKs — multi-person-month work with a functionality freeze
and a new team-wide mental model, introducing new bugs while fixing none of the above.
The equivalent capabilities already exist in lighter form (`app.onStop` for resource
cleanup, `AppError` + error-mapping for errors, native Promises for concurrency); what's
missing is discipline on existing patterns, delivered by plans 010–012. If typed errors
are desired, adopt `neverthrow` locally in the provider layer rather than Effect globally.
A scoped Effect.TS experiment inside a _new, isolated_ subsystem (e.g. a future relay
orchestration layer) would be reasonable; a whole-codebase migration is negative-value.

## Findings considered and rejected (so they aren't re-audited)

- **Git subprocess command injection** — `git-command.ts` uses `spawn` with arg arrays; injection-resistant. Not a finding.
- **Workspace file path traversal** — `workspace/files.ts` already uses `resolveWorkspaceFilePath` + `isWithinRoot`. Correct.
- **External provider list leaking secrets** — returns `credentialRef`, not raw values. Correct.
- **`test-reset` route exposure** — gated on `NODE_ENV === 'test'`. Acceptable.
- **Graceful shutdown not calling `abortAllRuns`** — mitigated by `recoverPersistedRunProjections` on next boot; low leverage. Noted in plan 012 maintenance, not planned separately.
- **Empty catches on chronicle download-progress / opportunistic PTY/Codex captures** — non-critical paths; lower leverage than the run-lifecycle catches in plan 012.
