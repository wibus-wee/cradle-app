# README
 
 This directory contains resources for Cradle, such as skills, tools, and agents.
 
 To use a resource in development, create a symbolic link to it in the appropriate directory under `.agents`. For example, to use a skill:

```
ln -s ../../resources/skills/<skill-name> .agents/skills
```