import type { StyleProp, ViewStyle } from 'react-native'

export interface NativeMarkdownComponentProps {
  markdown: string
  streaming: boolean
  style?: StyleProp<ViewStyle>
}

export function NativeMarkdown(_props: NativeMarkdownComponentProps) {
  return null
}
