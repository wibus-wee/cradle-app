import { ConnectionSettingsContainer } from '@/features/connection/ConnectionSettingsContainer'

export default function TokenSettingsRoute() {
  return <ConnectionSettingsContainer setting="token" />
}
